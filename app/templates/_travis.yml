language: node_js
notifications:
  email: false
node_js:
  - 'node'
  - '6'
  - '4'
before_install: if [[ `npm -v` < 3* ]]; then npm i -g npm@^3; fi<% if (esnext) { %>
before_script: npm run build<% } %><% if (coverage) { %>
after_script:
  - npm install -g codecov
  - codecov<% } %>
branches:
  only: master
