language: node_js
notifications:
  email: false
node_js:
  - '8'
  - '6'
  - '4'<% if (coverage) { %>
after_success:
  - npm install -g codecov
  - codecov<% } %>
branches:
  only:
    - master
    - /^greenkeeper/.*$/
