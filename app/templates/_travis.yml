language: node_js
notifications:
  email: false
node_js:
  - 'node'
  - '6'
  - '4'<% if (esnext) { %>
before_script: npm run build<% } %><% if (coverage) { %>
after_success:
  - npm install -g codecov
  - codecov<% } %>
