language: node_js
notifications:
  email: false
node_js:
  - '10'
  - '8'
  - '6'<% if (esnext) { %>
script:
  - npm test
  - npm run build<% } %><% if (coverage) { %>
after_success: if [[ `npm -v` > 5* ]]; then npx codecov; fi<% } %>
branches:
  only:
    - master
    - /^greenkeeper/.*$/
