language: node_js
notifications:
  email: false
node_js:
  - '8'
  - '6'
  - '4'
after_success:
  - if [[ `npm -v` > 4* ]]; then npx codecov; fi
  - npm run semantic-release
branches:
  only:
    - master
    - /^greenkeeper/.*$/
