language: node_js
notifications:
  email: false
node_js:
  - '8'
  - '6'
after_success:
  - if [[ `npm -v` > 4* ]]; then npx codecov; fi
  - npm install -g semantic-release@11
  - semantic-release
branches:
  only:
    - master
    - /^greenkeeper/.*$/
